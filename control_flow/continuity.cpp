#include "C:\Users\Admin\C++\old_headers\stuffs.h"
void code(void)
{
char done, ch;
done = 0;
while(!done) {
    if(kbhit())
{
    switch(getch()){
        case '$':
            done=1;
            continue;
        default:
            cout<<(char)()
    }
}
}
cout<<(char)(ch+1)<<endl; /* shift the alphabet one
position higher */
}
}


int main(){
    code();
}